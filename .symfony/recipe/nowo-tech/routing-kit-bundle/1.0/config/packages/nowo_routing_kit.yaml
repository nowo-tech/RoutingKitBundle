# Default configuration for RoutingKitBundle (Flex recipe)
nowo_routing_kit:
    enabled: true
    default_locale: en
    locales: [en]
    panel:
        enabled: true
        path_prefix: '/_routing'
    auto_invalidate_cache: true
    seo_kit_bridge: true
